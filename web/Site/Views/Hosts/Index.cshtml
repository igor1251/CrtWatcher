@model HostsViewModel
@{
    ViewData["Title"] = "Хосты";
}

<div>
    <fieldset>
        <legend>Зарегистрированные ПК</legend>
        <table class="table table-hover table-sm table-stripped">
            <thead class="thead-dark">
                <tr>
                    <th>IP</th>
                    <th>Имя ПК</th>
                    <th>Порт</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var host in @Model.AvailableHosts)
                {
                    <tr>
                        <td>@host.IP</td>
                        <td>@host.HostName</td>
                        <td>@host.ConnectionPort</td>
                    </tr>
                }
            </tbody>
        </table>
    </fieldset>
</div>
